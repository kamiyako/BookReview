  <% if current_user.following?(user) %>
    <%= link_to 'フォロー外す', public_user_relationships_path(user.id), class: "btn btn-danger", method: :delete, remote: true  %>
  <% else %>
    <%= link_to 'フォローする', public_user_relationships_path(user.id), class: "btn btn-success", method: :POST, remote: true %>
  <% end %>